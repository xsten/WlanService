<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="net.stenuit.xavier.wlanservice"
    android:versionCode="101"
    android:versionName="1.01" >
	<!-- 1.00 first commited version -->
	<!-- 1.01 removed unuseful button from gui -->
    <uses-sdk
        android:minSdkVersion="8"
        android:targetSdkVersion="17" />
        <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
        <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <service
        	android:name="net.stenuit.xavier.cetrelwlanservice.WlanService"
        	android:enabled="true">
    	</service>
    	<activity
            android:name="net.stenuit.xavier.cetrelwlanservice.MainActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    	<activity android:name="net.stenuit.xavier.cetrelwlanservice.SettingsActivity"></activity>
    </application>


</manifest>